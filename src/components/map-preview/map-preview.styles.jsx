import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  mapImage: {
    height: '100%',
    width: '100%',
  },
  mapPreview: {
    alignItems: 'center',
    justifyContent: 'center',
  },
})

export default styles;